out/
cache/
node_modules/
broadcast/
.env
.env.*
.deployer.key
.deployer.*.key
# private deployment record: chain registry, per-generation addresses + journal, ops secrets
.deploy/
*.local.md
package-lock.json
.DS_Store

# local ops tooling (deploys, verification, probes, sweeps) — the repo ships
# only the code and what's needed to run the tests. The root package.json only
# serves the deploy scripts (tests run through forge); web/package.json stays.
scripts/*
!scripts/run-external.sh
web/scripts/*
!web/scripts/gen-abi.mjs
!web/scripts/snapshot-pools.mts
/package.json
